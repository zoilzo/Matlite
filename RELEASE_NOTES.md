# MatLite v1.8.0

**发布日期：2026-09-26**

深度计算版本。共 5 大新能力 + 可复现工作流。共 59 个内置工具。

## 🟢 深度计算（新增）

### 矩阵分解
- **LU 分解**：A = P·L·U，显示下三角 L、上三角 U 及置换信息，必要时给出验证
- **QR 分解**：A = Q·R，显示正交矩阵 Q 与上三角 R，并验证 Q·R 重建 A
- **SVD 分解**：A = U·Σ·Vᵀ，显示 U、奇异值对角阵 Σ、V，并验证重建

### 常微分方程（ODE）数值解
- 输入 dy/dx = f(x,y) 与初值，自动画方向场底图
- 用 scipy `solve_ivp`（RK45 自适应）或欧拉法求解初值问题，叠加解曲线
- 自动给出端点值（如 y(x_end) ≈ 7.389，即 e²）

### 方向场（斜率场）
- 绘制 dy/dx = f(x,y) 的单位长度斜率场，直观展示曲线走向
- 可调网格密度，支持按初始点观察轨线

### 任意奇点留数
- 自动列出函数的全部孤立奇点
- 对每个奇点逐一求留数（不再只算 z=0）
- 可在「求值点 z0」填任意复数点，求该点留数

## 📦 可复现工作流（新增）

每个计算页计算结果后，点「📦 导出复现」：
- 自动捕获当前全部输入（矩阵 / 函数 / 区间 / 初值 / 方法）
- 生成**可独立运行的 .py 脚本**——用相同输入算出完全相同的结果
- 生成**人类可读的 .md 配方**——问题、输入、结果、工具版本（交作业/笔记）

## ✨ 回归加固

- 复变函数：留数功能重写，任意奇点 + 指定点（修复原固定 z=0 的局限）
- 数值计算：新增 ODE 与方向场两类模式，复用安全求值（SAFE）

## ⬇️ 下载

- [MatLiteSetup.exe（安装包）](./MatLiteSetup.exe)
- [MatLite-便携版.zip（免安装）](./MatLite-便携版.zip)

---
*Made by zoilzo & Claude*