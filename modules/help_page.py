# -*- coding: utf-8 -*-
"""帮助中心：快速上手 / 各模块教程 / 常见问题 / 关于与许可，全部用文本盒显示（无竖排问题）。"""

import customtkinter as ctk

from modules.app_settings import APP_VERSION

APP_NAME = "MatLite 数学工作台"


class HelpPage(ctk.CTkFrame):
    def __init__(self, master):
        super().__init__(master, fg_color="transparent")
        self.grid_columnconfigure(0, weight=1)
        self.grid_rowconfigure(0, weight=1)

        frame = ctk.CTkFrame(self, corner_radius=12)
        frame.grid(row=0, column=0, sticky="nsew", padx=12, pady=12)
        frame.grid_columnconfigure(0, weight=1)
        frame.grid_rowconfigure(0, weight=1)

        self.box = ctk.CTkTextbox(frame, font=ctk.CTkFont(size=13), wrap="word")
        self.box.grid(row=0, column=0, sticky="nsew", padx=8, pady=8)

        self._build()

    # ---------- 用文本填充 ----------
    def _h1(self, text):
        self.box.insert("end", "\n" + "=" * 40 + "\n" + text + "\n" + "=" * 40 + "\n\n")

    def _h2(self, text):
        self.box.insert("end", "\n「" + text + "」\n" + "-" * 30 + "\n\n")

    def _p(self, text):
        self.box.insert("end", "　" + text + "\n\n")

    def _b(self, text):
        self.box.insert("end", "　• " + text + "\n")

    def _build(self):
        b = self.box
        b.insert("end", f"{APP_NAME} v{APP_VERSION}\n")
        b.insert("end", "面向不懂代码的人：MATLAB 风格的数学与统计工作台\n")
        b.insert("end", "made by zoilzo & Claude\n")

        self._h1("🚀 快速上手")
        steps = [
            "第 1 步：从左侧选择要用的功能（公式/矩阵/绘图/统计等）。",
            "第 2 步：在左下方参数区填写函数、数据或变量。",
            "第 3 步：绿色「⚡ 计算」/「📈 绘图」/「▶ 运行分析」按钮出结果。",
            "第 4 步：右上方显示结果，多数还能看到公式或图形预览。",
            "第 5 步：需要 AI 解读时，先跑一次分析，再去 AI 助手点「解读当前分析」。",
            "第 6 步：登录后所有操作自动记入「历史记录」，可搜索/回看/导出。",
            "第 7 步：有问题或建议，点左上方「📮 意见反馈」，一键发到开发者邮箱。",
        ]
        for s in steps:
            self._b(s)

        self._h1("📘 各模块教程")
        mods = [
            ("🧮 公式计算", "输入表达式算数值/求导/积分/解方程/极限。函数用 sin、cos、sqrt、exp、ln、lg(常用对数)、log2；常数 pi、e；乘方用 x**2 或 x^2。左上角切换“计算类型”。"),
            ("🔢 矩阵与线性代数", "在格子里填数字（可填分数如 1/2），设好行数列数。加减乘要勾「使用矩阵 B」；解方程要勾「解方程组」并填 b 向量。点下方按钮出结果，分数精确显示。"),
            ("🔬 方程与数值计算", "求根：填 f(x) 和区间，自动二分/牛顿求根并画图。积分：填 f(x) 和上下限。线性规划：目标系数如 3,5，约束每行写 a1 a2, b。拟合：填 X、Y 数据选模型。"),
            ("∫ 微积分深化", "泰勒展开：在 a 点把函数展开成多项式。微分方程：输入 dy/dx=f(x,y) 和初值。参数方程/极坐标：给函数和范围画曲线。"),
            ("🎲 概率与分布", "分布计算：选正态/二项/泊松等，填参数和区间算概率。随机数生成：按分布产生样本看直方图。分布拟合：给数据自动找最贴合分布。"),
            ("📈 绘图可视化", "支持 2D 函数、3D 曲面、3D 散点、参数曲线、极坐标、等高线、散点折线柱状直方图。可拖动旋转 3D、画完保存 PNG、自动进历史。"),
            ("🔮 复变函数", "输入复函数 f(z)（如 z**2+1、exp(z)、1/z），可求值、画 |f(z)| 3D 曲面、复平面着色映射（色调=辐角）、泰勒展开、留数。坐标写法 1+i、2-3j。"),
            ("⏳ 时间序列", "粘贴一列时序数值，做数据折线 / 趋势-季节-残差分解 / ACF·PACF；或输入 ARIMA 阶数(p,d,q)拟合并预测、画 95% 置信区间。"),
            ("🧠 机器学习", "每行一个样本（空格分隔）；决策树时最后一列是类别标签。KMeans 按距离把样本自动分成 K 簇；决策树画树结构+混淆矩阵+特征重要性。"),
            ("📊 数据分析", "导入 Excel/CSV，做描述统计/正态性/t 检验/回归/卡方/ANOVA/相关/配对 t。结果表格对齐显示，可 AI 解读、导出 Word 报告。"),
            ("🤖 AI 助手", "连接本地 Ollama。点「刷新」选模型，输入问题或「解读当前分析」。默认会自动调用内置功能模块精确计算（求导/积分/解方程/矩阵/统计检验/概率/绘图/时序/机器学习/数值求根/曲线拟合/复变函数/Word 报告导出/图片识别），模型算完后用大白话解释结果；左侧开关可关掉自动调用。用 LM Studio 等就把接口改成“OpenAI 兼容”并在地址末尾加 /v1。"),
            ("🎯 拍照识题", "选择或粘贴题目图片，调用本地视觉模型（Ollama，如 qwen3-vl）转录并转成 Python 表达式，可修正后 求导 / 积分 / 画图。全程本地，图片不上传。"),
            ("🗂 历史记录", "登录用户自动记录：计算/矩阵/分析/绘图/操作日志。按类型浏览、关键词搜索、点记录看详情、导出/清空。游客不记录。"),
        ]
        for name, desc in mods:
            self._h2(name)
            self._p(desc)

        self._h1("❓ 常见问题")
        faq = [
            ("Q: 需要联网吗？", "全部计算在你本机完成，默认不联网。可选开启「公告与更新提醒」（设置页可关），它只拉取公告/版本信息，不上传任何数据。"),
            ("Q: 数据安全吗？", "数据只在你本机处理，历史、配置都存在本地，不会上传到任何服务器。"),
            ("Q: AI 回复慢？", "8B 大模型在普通显卡上要几十秒，属正常。可换更小的模型（如 deepseek-r1:1.5b）会快很多。"),
            ("Q: AI 助手怎么算数学题？", "开启左侧「自动调用内置功能模块」后，AI 会先调用 MatLite 内置计算模块得到精确结果，再用通俗语言解释。所有计算都在本机完成。"),
            ("Q: 报“连接失败”？", "确认 Ollama 已启动（ollama serve）、地址正确、模型已下载。程序已自动绕过系统代理。"),
            ("Q: 结果数字看着乱？", "到「设置」里可调小数位数、表格预览行数。"),
            ("Q: 忘记密码怎么办？", "本地账号暂不支持找回；可改用“游客模式”，或新建一个账号。"),
            ("Q: 怎么反馈问题？", "点左上方「📮 意见反馈」，SMTP 直发开发者邮箱：17588853057@163.com（内容同时存本地备份）。"),
        ]
        for q, a in faq:
            self._b(q)
            self._p("　　" + a)

        self._h1("📜 安装条款 / 许可协议")
        lc = [
            "一、授权：本软件可免费用于个人学习、作业与研究；商用或再分发前请联系开发者。",
            "二、免责：计算结果仅供参考，不构成任何决策依据；研究者须自行核验并承担使用后果，尤其 AI 助手输出仅供启发。",
            "三、隐私：数据、历史、设置均存于本机；意见反馈通过你填写的发件邮箱直接发送到开发者邮箱，不含任何自动上传。",
            "三、隐私补充：本软件默认不联网；若开启「公告与更新提醒」，仅会请求设置页填写的公告地址以获取新公告/版本信息，不上传任何用户数据，且可在设置中一键关闭。",
            "四、版权：© zoilzo & Claude。保留所有权利；随软件分发的第三方开源库遵循其各自许可协议。",
            "五、数据：AI 可选用本地 Ollama 模型，模型与对话内容不脱离本机。",
            "﻿联系方式：zoilzo <17588853057@163.com>",
        ]
        for x in lc:
            self._p(x)

        self.box.configure(state="disabled")